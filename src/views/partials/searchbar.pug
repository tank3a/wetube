form(method="GET", action="/search").search_bar
    if keyword
        input(placeholder="Search by Title", type="text", name="keyword", id="searchInput", value=keyword)
    else 
        input(placeholder="Search by Title", type="text", name="keyword", id="searchInput")

    button(type="submit", id="searchSubmit")
        i.fas.fa-search.fa-lg
